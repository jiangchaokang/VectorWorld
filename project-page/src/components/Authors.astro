---
import type { Author } from "../types/types";

interface Props {
  authors: Author[];
}

const { authors } = Astro.props;

const institutions: string[] = [];
const institutionMap = new Map<string, number>();

for (const author of authors) {
  const institution = author.institution?.trim();
  if (!institution) continue;

  if (!institutionMap.has(institution)) {
    institutionMap.set(institution, institutions.length + 1);
    institutions.push(institution);
  }
}

function getSuperscripts(author: Author) {
  const parts: string[] = [];

  const institution = author.institution?.trim();
  if (institution) {
    const index = institutionMap.get(institution);
    if (index) parts.push(String(index));
  }

  const noteSymbols = (author.notes ?? [])
    .map((item) => item.trim())
    .filter((item) => item.length > 0);

  noteSymbols.forEach((item) => parts.push(item));

  return parts;
}

const authorBaseClass =
  "inline-flex items-center rounded-full border border-hairline bg-surface-1 px-4 py-2.5 text-sm font-semibold text-ink-1 backdrop-blur transition duration-200";

const authorInteractiveClass =
  "hover:-translate-y-0.5 hover:border-brand hover:text-brand";

const affiliationClass =
  "inline-flex items-center gap-2 rounded-full border border-hairline bg-surface-2 px-3 py-1.5 text-sm text-ink-2";
---

{
  authors.length > 0 && (
    <div class="not-prose w-full max-w-4xl">
      <div class="flex flex-wrap items-center justify-center gap-3">
        {authors.map((author) => {
          const superscripts = getSuperscripts(author);

          return author.url ? (
            <a
              href={author.url}
              target="_blank"
              rel="noreferrer"
              aria-label={author.name}
              class:list={[authorBaseClass, authorInteractiveClass]}
            >
              <span class="tracking-tight">
                {author.name}
                {superscripts.length > 0 && (
                  <sup class="ml-1 text-xs font-semibold text-brand">
                    {superscripts.join(",")}
                  </sup>
                )}
              </span>
            </a>
          ) : (
            <span class={authorBaseClass}>
              <span class="tracking-tight">
                {author.name}
                {superscripts.length > 0 && (
                  <sup class="ml-1 text-xs font-semibold text-brand">
                    {superscripts.join(",")}
                  </sup>
                )}
              </span>
            </span>
          );
        })}
      </div>

      {institutions.length > 0 && (
        <div class="mt-4 flex flex-wrap items-center justify-center gap-2.5">
          {institutions.map((institution, index) => (
            <div class={affiliationClass}>
              <span class="inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-brand px-1.5 text-xs font-semibold text-brand-contrast">
                {index + 1}
              </span>
              <span>{institution}</span>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}