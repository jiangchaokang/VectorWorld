defaults:
- base

run_name: scenario_dreamer_autoencoder_waymo # from base.yaml
max_steps: 85000 # from base.yaml
warmup_steps: 500 # from base.yaml
lr_schedule: linear # from base.yaml
weight_decay: 2e-5 # from base.yaml
check_val_every_n_epoch: 2 # from base.yaml
num_samples_to_visualize: 2 # from base.yaml
viz_dir: outputs/viz/val_${ae.train.run_name} # from base.yaml
devices: 1 # from base.yaml
resume_ckpt_path: null
init_ckpt_path: null
